import { View, Text, TextInput, Image, ScrollView, StyleSheet } from 'react-native';

export default function App() {
  return (
    <ScrollView>
      <View style={styles.container}>

        <Text style={styles.title}>College App</Text>

        <Image
          source={{ uri: 'https://via.placeholder.com/150' }}
          style={styles.image}
        />

        <TextInput placeholder="Student Name" style={styles.input} />
        <TextInput placeholder="Roll Number" style={styles.input} />

        <Text style={styles.text}>Welcome to our College</Text>

      </View>
    </ScrollView>
  );
}

const styles = StyleSheet.create({
  container: {
    padding: 20
  },
  title: {
    fontSize: 24,
    textAlign: 'center'
  },
  image: {
    width: 150,
    height: 150,
    alignSelf: 'center',
    margin: 10
  },
  input: {
    borderWidth: 1,
    padding: 8,
    marginVertical: 5
  },
  text: {
    textAlign: 'center'
  }
});
